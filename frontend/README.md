# Draft Coach frontend

React + TypeScript. Use `npm run dev:local` and `npm run build:local`. See the project root README for the Python backend, API configuration, and Mac startup. Local output lives in `out/`; `build/` contains Sites build tooling and must not be used as an output directory.
